# The following lines of boilerplate have been extracted from the CMakeLists.txt file that
# was originally installed by the ESP-IDF. These lines are necessary to build this
# component with the ESP-IDF build system.

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(wifite32)
